import React from "react";
import ReactDOM from "react-dom/client";
import "../shared/styles.css";
import { OptionsApp } from "./ui";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <OptionsApp />
  </React.StrictMode>,
);
